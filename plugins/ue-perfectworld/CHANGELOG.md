# UE PerfectWorld 更新日志

## 0.4.4 - 2026-09-17

- `ue-core-rules.md` 新增 Integration Timing Maps 节：接入共享框架前必须绘制完整时序图（横轴时间、纵轴参与者、标注每个关键函数的实际执行时刻），同步调用与异步回调用不同线型。事故案例：`PrepareExecution` 在 Montage 结束时才运行，时序模型错误导致两轮返工——读函数做什么不等于读函数什么时候跑。
- `ue-architecture.md` 新增 Implicit Contract Enumeration 节：接入方与框架原始调用方形态不同时（同步 vs 异步、有目标 vs 无目标、常驻 vs 一次性），逐条枚举框架入口每道闸（校验、合同、能力声明、时序要求、资源租约、生命周期通知），标注接入方是否满足。成熟框架的隐性契约数量级 8-12 条；契约串行暴露（修一条暴露下一条），按已过闸数追踪进度而非按修复轮数。
- `ue-paste-code-gate.md` 新增第 2 条 API 参数语义从实现读取：调用既有 API 时读到其实现的完整分支逻辑后再决定参数值。签名只说类型不说语义约束（"传无效 GUID 走创建、传有效但未注册的走复用"只在实现里）。新增第 7 条文档交付的升级标准：交付形态为可粘贴文档时查证标准提高一级——直接编辑有编译器做免费第一轮，文档交付没有，验证手段少了人工验证必须补上。质量门禁跟着代码走不跟着交付形态走。
- `ue-bugfix-discipline.md` 新增 Serial Gate Debugging 节：多闸串行链路的调试特征是修好一条暴露下一条，这是进展不是回归。按已过闸数追踪进度（"第 7 道共 10 道"可指导行动，"第 6 轮还没修好"只会打击士气）。验证前一个修法是否生效：看前一个失败模式是否仍复现，不复现说明修好了、新失败是下游闸。

## 0.4.3 - 2026-09-16

- 新增 `ue-paste-code-gate.md` 贴码门禁 reference，`ue-implement` 与 `ue-draft` 均纳入必读：凡生成将贴入项目的代码——直接编辑或 Markdown 规格交付两种形态皆适用——必须逐符号验证后交付。依据：贴码规格没有编译器兜底，符号级错误全部转嫁给贴码人、且只在对方编译期暴露，验证必须前置到生成时。
- 符号查证三查：新增符号全模块 grep 查重（含既有测试访问器与编辑器辅助结构）；引用符号 grep 声明处确认可见性（public/private/friend）、性质（方法/委托成员/数据成员）与完整签名，禁止凭命名惯例推断；include 按同模块现成先例照抄，禁止按目录结构拼路径。
- 完整读取强制：整函数替换必须读到函数结尾大括号，`grep -A` 截断输出不得当作函数全貌（事故案例：函数尾部清理段与状态标志被截断丢失，导致波次推进挂死）；定位锚点当轮现场生成，禁止跨轮次、跨头文件与实现文件复用读取记忆。
- 交付自检三查：全部改动按符号引用排出贴码顺序并逐批验证可独立编译；每个小节标题携带目标文件全名（.h/.cpp 明确，头文件与实现文件插入点分开指定）；无法静态验证的项显式标注现场校准，禁止占位断言与编造内容。
- 本门禁预防的失败类别清单化：同名符号冲突、私有接缝访问违规、方法误判为委托、include 路径拼错、尾部逻辑丢失、陈旧锚点、批次断链。

## 0.4.2 - 2026-09-15

- `ue-comment-log-rules.md` 按团队实战注释规范整体重写：注释面向"没看过这段代码、快速扫读"的程序员，只写代码说不出来的东西（意图、契约、所有权、单位、失败行为），一眼可懂、不叙述语法。
- 注释一律使用 `//`，不使用 `/* */` 与 `/** */`；结尾不加句号。简洁但不空洞：`// 处理数据` 为零分，`// 按 FX 表行 ID 查池组件，查不到走兜底` 为合格。
- 函数内注释改为强制分布式：多逻辑步骤的函数体每个逻辑块（校验、分支、异步绑定、状态转换、清理）各配一行注释，仅极短自明函数体可免；禁止在函数开头或任何一处堆多行注释块交代所有事情，只有拆不开的复杂契约例外。可检验标准：只读各行单行注释即可重建函数执行逻辑。
- 新增声明与实现对齐规则：`.h` 声明与 `.cpp` 实现上方都必须有一行用途注释，两处一字不差、同改同删；文件内本地辅助函数只注定义处。
- 修改标记格式固化为 `// <作者> begin: <一句话说明本次改动>` … `// <作者> end`：跟随项目既定标记规范、不自创风格，标记随代码删除，不留空标记对。
- 新增蓝图暴露 API 注释要求：`UPROPERTY`/`UFUNCTION` 注释即编辑器 tooltip，按策划视角写配置项、单位、取值范围与默认行为。
- 新增数字单位与哨兵值说明、异步调用完成预期、禁止保留注释掉的死代码、变通方案必须注明成因与移除条件等要求；正反例改为项目实战代码（分布式单行注释＋标记包裹 vs `/** */` 开头大段叙述）。

## 0.4.1 - 2026-09-15

- 新增 `ue-dev-doc` 技能：为一次完成的 UE 改动撰写团队风格的开发文档（飞书「🗒️开发文档」定稿格式）。骨架为：对应需求与一句话总结、背景与目标（编号痛点＋设计取向理由）、改动面表格（文件｜改动）、按模块分章详解、影响面论证、"既有路径未变化"零改动证明、生命周期与清理。
- 枚举类内容（检测清单、组件构成、触发源、状态语义、环境防护矩阵）一律表格化；每个关键决策的"为什么"贴机制约束写。
- 铁律：文档中的文件名、函数名、枚举名、行为描述必须来自用户提供的改动、会话证据或项目检索；无依据处标【待核对】，不编造。
- 编辑器专属代码要求逐环境交代 PIE／编辑器游戏世界／打包版／命令行／专用服的行为与防护。
- 共享基础设施变更的影响面论证接入 `ue-architecture.md`；RAG 可用时按 `ue-rag-integration.md` 先定位再核对。

## 0.4.0 - 2026-09-10

结构精简（清除重构残留，收敛双份维护）：

- 删除 `ue-complete-implementation.md`：内容与变更规格工作流和证据门重复，独有条目（复制的表行需跨平台模板核对）并入 `ue-project-consistency.md`。
- `ue-plan` 并入 `ue-draft`：两者同为"先证据后输出"的只读流程，合并后按请求形态分级输出（方案级 / 可粘贴补丁级），少维护一个技能。
- 删除 `ue-router`：路由表与各技能 description 重复维护；两条独立有价值的架构路由规则（共享能力不以首个调用方定 owner、异步先过架构门再谈 Tick）并入 `ue-core-rules.md`。
- 公共必读参考集收敛到 `ue-core-rules.md` 统一维护，各技能只声明自己的差异参考，消除七份清单各自漂移的维护面。

能力补充（来自一次完整实战复盘的七个短板）：

- 先例可信度门：代码普查必须过滤测试、工具、命令行、注释代码；普查结论与具名评审裁决冲突时以人类裁决为准，规范争议向所有者确认而不是数出现次数。
- 搜索自检：符号"不存在"的断言必须先用已知正例做对照验证；补充 Windows shell 模式陷阱（`/` 开头模式的路径转换、内嵌双引号吞字符、`rg -v` 按子串误杀合法调用行）。
- 补丁完整性协议：聊天交付补丁要求编号 + ASCII 锚点 + 承重标记，同函数多块时优先整函数替换，随附"移除符号清单"；用户应用后必须复读触区并做残差扫描（并入 `ue-edit-safety.md` 与审查流程，半应用补丁成为独立审查发现类别）。
- 编译门：用户授权构建验证时优先增量单模块构建，而非全量构建。
- 资产保活契约：preload / 异步加载设计必须声明保留持有者（streamable 句柄 / 强引用 / 缓存条目）与"加载完成→首次使用"之间的 GC 窗口，审计 fire-and-forget 默认参数（并入 `ue-architecture.md`）。
- 工作区拓扑：先定位 `.uproject` / `Config` 根；当代码仓库不是工程根（Git/SVN 双仓库），配置类变更必须声明落点仓库与两边提交的配对关系。
- git 授权边界：版本控制变更（commit / reset / push / 文件级 checkout）需逐次获得用户同意，聊天交付偏好自动延伸到 git 操作。

## 0.3.1 - 2026-09-09

- 新增公共 UE 基础设施架构规则：先区分局部业务与共享能力，再确定实现边界和公共 API。
- 明确异步资源加载、初始化、注册和失败等一次性状态转换应优先使用事件、委托、回调或现有异步完成接口。
- 明确 Tick 仅用于持续推进、跟踪、插值和模拟；不得把每帧轮询作为共享异步资源等待的默认方案。
- 增加池化异步资源的缓存命中、失败、取消、回池、复用、句柄和生命周期校验要求。
- 规划、路由、实现、排查和审查流程接入公共基础设施架构门禁。
- 新增 `ue-code-style.md`，将团队 C++ 可读性约定接入相关工作流，并区分阻断项、默认建议和上下文相关建议。
- 明确不强制未定稿的 `m_`、`g_`、布尔前缀、`auto`、foreach、除法或平方根规则；必须以当前项目证据和 Unreal 生成代码约束为准。

## 0.3.0 - 2026-08-06

- 新增与 `ue-project-rag` 的可选联动。大型项目可先检索符号、配置、文档和资产元数据，再缩小后续调查范围。
- RAG 只承担候选发现：检索评分、摘要和资产元数据不能证明所有权、生命周期、网络权威、持久化、清理或 Blueprint 运行时行为。
- 规划、排查、直接实现、审查、Blueprint 集成和只读变更规格工作流都会在 RAG 工具可用时加载联动规则；工具不可用时继续使用直接项目搜索。
- 将未授权写入时的交付收敛为变更规格，明确已验证证据、变更面、依赖、风险和验证条件。
- 更新插件描述与关键词，使其反映项目证据优先和本地检索协作的定位。

## 0.2.0 - 2026-07-27

- 新增现有能力准入门禁：在允许新机制前检查完整类型继承、继承编辑器属性、Blueprint Designer 配置、项目数据、路由和活跃生产调用者。
- 要求将每项职责分类为 `reuse`、`configure`、`extend` 或 `create`；只有当前项目证据排除兼容的复用与配置路径后，才允许扩展或创建。
- UI 交互和表现优先采用现有控件配置、继承组件或基类属性、表或路由配置和既有辅助函数，其次才是局部代码和共享框架修改。
- 新 DataAsset、红点身份、存档结构、RPC、代理状态、委托、管理器、同步路径和生命周期覆写若只服务于展示或导航，将触发阻断式复杂度检查。
- 后续证据发现项目已有能力时，必须删除为先前错误能力缺口引入的补偿机制，不能保留并行实现作为兼容路径。
- 准入门禁继续集中在项目一致性和最终自审规则中，所有 UE 工作流自动继承，没有增加新的技能或规则文件。

## 0.1.9 - 2026-07-25

- 新增完成声明门禁，将责任证据矩阵和变更面清单转换为已验证、已否证、不可验证或不适用的明确状态。
- 存在必要职责或执行阶段被否证、不可验证时，禁止声称实现完整、最终、就绪、最佳或审查没有问题。
- 最终结论必须说明实际达到的最高验证等级，禁止把源码检查描述为编译、运行、网络权威、持久化、跨会话或跨平台验证。
- 增加针对缺失、格式错误、陈旧、重复、未激活、重复输入、延迟或乱序回调、重新加载、重连和清理等适用失败场景的集中检查。
- 门禁继续复用现有证据和最终自审规则，所有 UE 工作流自动继承，不增加新的规则文件或具体功能策略。

## 0.1.8 - 2026-07-24

- 明确拆分状态生产与持久化所有者、确认或消费所有者，以及只负责展示的消费者。
- 多个界面或系统展示同一状态时，必须复用同一权威身份，禁止无依据地增加代理状态、转发事件、镜像存档或同步代码。
- 将 `NEW`、角标、数量和不同 Brush 等视觉差异默认视为表现层，只有经过验证的业务规则才能把它们定义为独立状态。
- 新增入口、配置、路由、目标解析、创建、表现、确认、持久化和清理的执行阶段映射，避免把不同阶段误判为重复系统。
- 新增阻断式变更面清单，将文件、资产、表行、Tag、注册点和 Blueprint 绑定标记为创建、替换、配置或仅复用；未声明的共享框架改动会重新触发能力匹配门禁。
- 最终自审扩展到 C++、工厂注册、DataAsset、DataTable、GameplayTag、Blueprint 绑定、UI 扩展与平台覆写、外部数据、持久化和清理的完整契约。
- 复制编辑器表行或资产时，要求保留已验证的隐藏字段，同时替换仍指向源功能的平台专用引用。

## 0.1.7 - 2026-07-24

- 在实现、补偿或扩大范围前新增阻断式需求与能力匹配门禁。
- 要求冻结用户最新需求和硬边界，删除只属于已废弃需求的行为、状态、兼容分支与扩展机制。
- 要求每项行为优先映射到已经拥有该职责的最高层项目能力，不能先假设缺少能力再自行补建局部系统。
- 当需求与允许范围冲突时，必须说明准确冲突、不可避免的严格实现成本和最接近的项目原生替代方案，交由用户决定，不能虚构局部补偿。
- 新增文件、共享集成点、状态所有者、持久化路径、RPC、缓存、委托、定时器、管理器或配置源时，必须重新执行匹配门禁。
- 最终自审新增过时行为、隐藏范围扩张和需求变化过程中累积的无用机制检查，并将门禁接入规划、排查、变更规格、直接实现、审查和 Blueprint 工作流。

## 0.1.6 - 2026-07-23

- 新增职责证据矩阵，禁止用一个相似功能同时证明 UI 组合、导航、状态、生命周期、防护和诊断等不同职责的实现方式。
- 要求项目专用的对象获取、空值防护、可选控件处理、回调校验和日志行为分别匹配同职责、同生命周期的活跃代码证据。
- 禁止统一采用“每个返回都记录日志”或“所有失败都保持静默”的策略，必须先区分失败类型并遵循项目已有处理路径。
- 新增遍历、直接管理器调用、绕过包装层、查找路径或日志策略时，必须重新打开项目调查，不能沿用无关先例。
- 最终自审新增重复边界日志、虚构 HUD/玩家访问方式、可选控件处理不一致，以及单纯为减少日志或行数而改变设计等阻断检查。
- 将职责证据门禁接入规划、排查、Blueprint 协作、变更规格、直接实现和审查工作流，没有增加重复规则文件。

## 0.1.5 - 2026-07-23

- 为非简单跨系统 UE 设计新增强制证据门禁，完整代码、编辑器配置、直接修改或框架扩展之前都必须通过。
- 要求分别证明权威数据或配置源、运行时所有者与生命周期、触发路径、读取或确认路径、持久化以及清理或过期路径。
- 禁止用相邻职责互相代替证据：父子聚合不能证明消息创建，UI 展示不能证明服务端持久化，触发路径不能证明清理路径。
- 要求将未解决问题拆分为可验证子问题，穷尽当前项目搜索和端到端追踪后，只向用户询问项目外部资料或真实业务决策。
- 缺少必要证据时必须明确报告缺口，不得虚构条件、管理器、DataAsset、字段、委托或生命周期钩子。
- 将证据门禁接入规划、排查、Blueprint 协作、变更规格、直接实现、审查、最终自审和连续失败恢复流程。

## 0.1.4 - 2026-07-23

- 将证据、所有权、可维护性和失败恢复要求合并进项目一致性、变更规格和最终自审的权威规则，删除三份重复 reference。
- 新增 `ue-complete-implementation.md`，统一管理只读变更规格、跨系统设计确认和验证要求。
- 将工作流规则改为“核心加按需”模型：网络、事务、UI 参数、编码、注释日志和 bug 修复规则只在相关任务中加载。
- 将路由器收敛为纯工作流选择，不再预加载会由已选技能再次读取的 reference。
- 将未授权写入的输出规范调整为项目证据、变更面和验证边界，减少与直接实现工作流的重叠。

## 0.1.3 - 2026-07-22

- 将插件清单版本更新为 `0.1.3`，引入证据与可维护性门禁。
- 对项目一致性、最终自审、调查、规划、实现、审查和 Blueprint 工作流补充所有权、生命周期与可维护性要求。
- 仅在用户明确授权时写入项目；未授权时保留完整的变更说明与验证边界。

## 0.1.2 - 2026-07-22

- 发布 `0.1.2`，补充客户端与服务端边界、异步回调和失败恢复检查。
- 扩展 bug 修复与最终自审规则，要求区分已证实的根因、未验证假设和未覆盖路径。

## 0.1.0 - 2026-07-17

- 首次发布 UE PerfectWorld，提供规划、排查、实现、审查、Blueprint 协作和工作流路由能力。
- 建立项目一致性、编辑安全、网络边界、UI 参数契约、注释与日志、变更漂移和最终自审等基础规则。

## 早期版本

## 0.0.9 - 2026-07-13

- 汇总规划、排查、实现和审查入口，形成首个可发布的 UE 工作流范围。
- 整理插件清单、资源和基础规则，为 `0.1.0` 首发准备发布结构。

## 0.0.8 - 2026-07-09

- 固化 Blueprint 与编辑器协作的任务边界。
- 补充 UI 参数、注释和日志等按需规则的接入方式。

## 0.0.7 - 2026-07-04

- 建立代码变更前的项目调查顺序，覆盖调用点、配置和相关资产。
- 将网络边界、生命周期和清理路径列为跨系统任务的必要核验项。

## 0.0.6 - 2026-06-29

- 形成以现有项目约定为先的实现原则。
- 区分可复用能力、局部扩展与新建机制，避免无依据的平行实现。

## 0.0.5 - 2026-06-25

- 定义 UE Bug 排查的证据收集和假设收敛方式。
- 约束最终结论，要求说明已验证范围与未覆盖风险。

## 0.0.4 - 2026-06-20

- 建立 Blueprint、C++、数据资产和编辑器配置的协作视角。
- 明确只读调查与授权写入之间的边界。

## 0.0.3 - 2026-06-16

- 梳理规划与实现阶段的输出结构。
- 将所有权、数据来源和触发链路纳入设计检查项。

## 0.0.2 - 2026-06-11

- 扩展从需求到变更面的基本调查框架。
- 引入对项目既有模式和相似实现的优先查找。

## 0.0.1 - 2026-06-07

- 建立 UE 专用工作流的最初范围：规划、排查、实现与审查。
- 确立以项目证据而非通用假设驱动建议的原则。

> 注：`0.0.x` 为开发阶段编号；可安装版本从 `0.1.0` 开始。

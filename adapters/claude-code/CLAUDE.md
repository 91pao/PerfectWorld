# PerfectWorld

读取 `<repo-root>/core/AGENT-CONTRACT.md` 并遵守其中的调查、授权和验证约定。

- 先读取 `<repo-root>/core/workflows/task-routing.md`，为当前任务选择主工作流。
- 通用工程任务读取 `<repo-root>/core/workflows/general-development.md`。
- Unreal Engine 任务读取 `<repo-root>/core/workflows/unreal-engine.md`。
- 可使用 UE Project RAG MCP 时，读取 `<repo-root>/core/policies/retrieval.md`。
- 涉及变更、审查或实现时，按 `<repo-root>/core/contracts/change-record.md` 汇报依据、影响、验证和未覆盖项。

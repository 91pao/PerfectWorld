# 本地检索规则

UE Project RAG 是标准 MCP 检索服务，可用工具为 `ue_rag_index`、`ue_rag_search`、`ue_rag_open` 和 `ue_rag_status`。

1. 先查询索引状态；不存在或已经失效时再构建索引。
2. 用具体的类名、函数、Gameplay Tag、配置键、资产名或报错文本检索。
3. 只打开最相关的少量命中项，随后直接读取原始项目文件或配置。
4. 在分支切换、大范围重构或资产元数据重建后刷新索引。

检索结果用于发现候选证据。相似度、摘要和元数据都不能替代原始文件、运行时追踪或项目配置核验。
